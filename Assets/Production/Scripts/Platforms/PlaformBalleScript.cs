using UnityEngine;

namespace Production.Scripts.Platforms
{
    public class PlaformBalleScript : MonoBehaviour
    {
    



        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
