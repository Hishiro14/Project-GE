using UnityEngine;

namespace Collaborators_Folder.Dylan
{
    public class GroundScript : MonoBehaviour
    {
        private int GroundHP = 3;
    
        // Start is called before the first frame update
        void Start()
        {
        
        }

        private void OnTriggerEnter2D(Collider2D other)
        {
        
        }
    }
}
